posterior <- extract(stan_test)
posterior$beta_c[1]
posterior$beta_c
head(posterior$beta_c[,1])
quantile(x = posterior$beta_c[,1], probs = c(.025,.975))
quantile(x = posterior$beta_c[,1], probs = c(.05,.95))
min(posterior$beta_c[,1])
min(posterior$beta_c[,1])
colMeans(posterior$v)
 nimble::simulate
